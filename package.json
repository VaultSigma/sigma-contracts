{
  "name": "@sigma/monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/contracts"
  ],
  "packageManager": "yarn@4.9.1",
  "scripts": {
    "account": "yarn workspace @sigma/contracts account",
    "account:generate": "yarn workspace @sigma/contracts account:generate",
    "account:import": "yarn workspace @sigma/contracts account:import",
    "chain": "yarn workspace @sigma/contracts chain",
    "compile": "yarn workspace @sigma/contracts compile",
    "deploy": "yarn workspace @sigma/contracts deploy",
    "flatten": "yarn workspace @sigma/contracts flatten",
    "fork": "yarn workspace @sigma/contracts fork",
    "format": "yarn workspace @sigma/contracts format",
    "lint": "yarn workspace @sigma/contracts lint",
    "test": "yarn workspace @sigma/contracts test",
    "verify": "yarn workspace @sigma/contracts verify"
  },
  "devDependencies": {
    "husky": "~9.1.6",
    "lint-staged": "~13.2.2"
  },
  "engines": {
    "node": ">=20.18.3"
  },
  "dependencies": {
    "shx": "^0.4.0"
  }
}
